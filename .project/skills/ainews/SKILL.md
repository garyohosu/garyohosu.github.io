---
name: ainews
description: ainews.md を読み、その指示に従ってAIニュース記事を作成する
---

# 目的

`ainews.md` に書かれた手順をそのまま実行し、AIニュースのブログ記事を作成する。
詳細ロジックはすべて `ainews.md` を正とする。このファイルは薄いラッパーである。

# 手順

1. プロジェクトルートの `ainews.md` を読む
2. そこに書かれた指示をそのまま実行する
3. 不足している情報がある場合のみ、最小限で補う

# 注意

- 記事構成・Web調査・期間判定・画像生成・出力形式はすべて `ainews.md` の定義が優先される
- このファイルに `ainews.md` の内容を重複して書かない（二重管理を避ける）
- `ainews.md` が更新された場合、このファイルの変更は不要
